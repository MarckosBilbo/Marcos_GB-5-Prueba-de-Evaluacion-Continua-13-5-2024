package Ej_1_DelExamen.Entrega_2;

/**
 * Interface for observers in the observer pattern.
 */
public interface Observer {
    /**
     * Method to update the observer, to be called when the subject changes.
     */
    void update();
}